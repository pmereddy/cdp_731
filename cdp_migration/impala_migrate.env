###############################################################################
# impala_migrate.env -- Configuration for Impala cross-cluster migration
###############################################################################

# ---------------------------------------------------------------------------
# Source Cluster  (the cluster you are migrating FROM)
# ---------------------------------------------------------------------------
SOURCE_IMPALA_HOST="source-impala-daemon.example.com"
SOURCE_IMPALA_PORT="21050"

# ---------------------------------------------------------------------------
# Target Cluster  (the cluster you are migrating TO)
# ---------------------------------------------------------------------------
TARGET_IMPALA_HOST="target-impala-daemon.example.com"
TARGET_IMPALA_PORT="21050"

# ---------------------------------------------------------------------------
# Shared EFS Storage
# ---------------------------------------------------------------------------
EFS_MOUNT="/mnt/efs/shared"
EFS_STAGING_DIR="/mnt/efs/shared/impala_migrate_staging"

# ---------------------------------------------------------------------------
# Kerberos  (kinit must be done before running the script)
# If using a keytab, set both variables; the script will kinit automatically.
# Leave empty to rely on an existing TGT in the credential cache.
# ---------------------------------------------------------------------------
KRB_PRINCIPAL=""
KRB_KEYTAB=""

# ---------------------------------------------------------------------------
# impala-shell extra options  (appended to every invocation)
# Examples: --ssl  --ca_cert=/path/to/ca.pem  --auth_creds_ok_in_clear
# ---------------------------------------------------------------------------
IMPALA_SHELL_EXTRA_OPTS=""

# ---------------------------------------------------------------------------
# Tool paths  (override if not on $PATH)
# ---------------------------------------------------------------------------
IMPALA_SHELL_BIN="impala-shell"
HADOOP_BIN="hadoop"
HDFS_BIN="hdfs"

# ---------------------------------------------------------------------------
# Data-movement tuning
# ---------------------------------------------------------------------------
# distcp bandwidth limit per mapper in MB  (0 = unlimited)
DISTCP_BANDWIDTH_MB="0"
# distcp max number of mappers
DISTCP_MAX_MAPS="20"

# ---------------------------------------------------------------------------
# Verification
# ---------------------------------------------------------------------------
# Number of sample rows to checksum (0 = checksum all rows)
VERIFY_SAMPLE_ROWS="10000"
